# Generate once with: openssl rand -base64 48
RSS_GENERATOR_WORKER_TOKEN=replace-with-a-random-internal-token

# Public HTTPS origin used when displaying RSS/Atom subscription URLs.
# Leave empty to return relative URLs; the management page resolves them against
# its current origin.
RSS_GENERATOR_PUBLIC_BASE_URL=https://feeds.example.com
